<%- include('layout/header') %>

<div class="container">
    <div class="row my-4">
        <div class="col-lg-12">

            <% if(message) { %>
                <div
                  class="alert alert-dismissible fade show alert - <%= message.type %>"
                  role="alert"
                >
                  <button
                    class="btn-close"
                    type="button"
                    data-bs-dismiss="alert"
                    aria-label="Close"
                  ></button>
                  <strong> <%= message.message %></strong>
                </div> 
                <% } %>

            <div class="table-responsive">

                <% if (category != "") { %>
                    <table class="table table-striped text-center my-5">
                        <thead>
                            <tr class="table-dark">
                                <th> ID </th>
                                <th> Category Name </th>
                                <th> Category Description </th>
                                <th> Action </th>
                            </tr>
                        </thead>
                        <tbody>
                            
                            <% category.forEach((row, index) => { %>
                                <tr class = "align-middle">
                                  <td><%= index %></td>
                                  <td><%= row.catName %></td>
                                  <td><%= row.catDescription %></td>
                                  <td>
                                    <a href="/editCategory/<%= row._id %>" class="text-success"><i class="fas fa-edit fa-lg mx-1"></i></a>
                
                                    <a href="/deleteCategory/<%= row._id %>" class="text-danger"
                                    ><i class="fa-regular fa-trash-can fa-lg mx-1"></i
                                    ></a>
                                  </td>
                                </tr>
                              <% }) %>

                        </tbody>
                    </table>
                    <% } else { %>
                        <h1 class="text-center text-secondary mt-5"> No Category Found :) </h1>
                        <% } %>
            </div>
        </div>
    </div>
</div>

<%- include('layout/footer') %>